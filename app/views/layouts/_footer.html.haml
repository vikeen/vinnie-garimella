%footer
  %nav
    %ul
      - if current_user
        %li Logged in as #{current_user.email}.
        %li #{link_to "Log out", log_out_path}
      - else
        %li #{ link_to "Log in", log_in_path }
